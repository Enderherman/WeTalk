## WebSocket Origin 白名单（2026-09-27）

- 浏览器 WebSocket 握手只接受 WETALK_WEB_ALLOWED_ORIGINS 中的 Origin；Origin: null 仅对旧 token 客户端兼容，不接受一次性 ticket。
- 无 Origin 的原生 Electron 客户端继续兼容；生产部署需把允许列表设为网页 HTTPS Origin。
- Maven clean verify 65 项测试通过，新增来源规范化、伪造来源拒绝和旧客户端兼容测试。真实后端原始握手验证伪造 Origin 收到 close 帧且无应用数据、ticket 可由允许来源继续消费，以及 Origin: null 的 Electron token 连接仍收到 INIT。

## 灰度更新身份校验与网页版本提示（2026-09-27）

- `/app/checkUpdate` 继续兼容旧版 uid 参数，但灰度资格改由认证会话中的用户编号判断，避免伪造其他用户 ID 查看灰度发布信息。
- 网页端读取 `VITE_WEB_VERSION`，展示已发布版本和更新说明；外链只允许 HTTP/HTTPS，新版提醒不提供桌面安装包下载。
- 真实后端验证全网发布、灰度用户、伪造 UID 拒绝和无更新结果；临时管理员、用户及发布记录已清理。Maven clean verify 61 项通过。


## AI 流式状态与停止接口（2026-09-27）

- AI 完成、用户停止和提供方失败均持久化到回复消息状态；停止时保存已输出的部分文本。
- 新增 `POST /api/chat/cancelAiMessage`，仅允许消息所属用户停止生成；重复调用返回现有结束状态。
- 提供方错误会通过 WebSocket 类型 16 和状态 3 通知客户端，前端可展示通用错误提示；进程重启后遗留的空占位消息可结束为失败状态。
- 真实后端以本机 OpenAI 兼容 SSE 模拟服务验证成功流、累计片段、取消、错误及历史读回；Maven clean verify 60 项通过。

# 后端更新记录

## 管理员机器人头像校验 — 2026-09-27

- 管理员保存机器人头像/封面前，校验扩展名、MIME 类型及文件签名；PNG、JPEG、GIF、BMP、WebP 单文件上限 10 MiB。
- 图片全部通过校验后才写入固定机器人文件名；允许单独更新封面，拒绝封面之外的无头像不完整数据；机器人账号编号在服务端强制保持 `Urobot`。
- 用户资料头像与封面复用同一校验器，增加文件签名识别。
- 4 项机器人图片控制器测试、3 项用户图片测试通过；真实后端验证头像/封面上传和鉴权读回、错误格式与超限文件拒绝、机器人编号固定。原头像/封面、系统设置和临时管理员均已恢复/清理。

## 群成员配额校验修正 — 2026-09-27

- 群成员上限改为读取 `maxGroupMemberCount`；此前误用了每人可创建群数 `maxGroupCount`，导致管理员设置的成员上限不生效。
- 添加回归测试：创建群数量配额与群成员数量配额分别生效。
- 真实后端验证：群主达到建群上限后新建群被拒；成员数为 1、上限为 2 时允许添加第一位成员，达到 2 后拒绝继续添加；图片设置为 1 MiB 时拒绝 1 MiB+1 的上传。原系统设置和临时账号、群、文件均已清理。

## 群聊会话生命周期 — 2026-09-26

- 成员退群或被移出时，删除该成员的群会话索引；群聊解散时清理全体成员的群会话索引。
- 群 WebSocket 事件仍实时发送给当前在线成员；之后重新初始化会话时，已退出/解散的群不再显示为可用会话。
- 群消息历史记录保留在数据库中；仅清理活动会话映射。
- 增加退群/移出和解散会话清理单元测试。

## 数据库命名统一 — 2026-09-26

- 默认数据库由 easychat 统一为 wetalk：JDBC、环境模板、MySQL Compose 初始化/健康检查及凭据准备、schema 导出脚本同步。
- 本机数据库先做 Git 忽略的私有全量备份，再整体迁移 9 张表；194 条记录的逐表行数与数据 CHECKSUM 均验证一致。保留空的旧 schema 及回退 SQL，未删除业务数据。
- 初始化 SQL 从更名后的 wetalk 重新导出，仅包含空表结构。
- 新增带预检、备份、校验和回退记录的手工数据库更名脚本；保留 easychat 作为旧库迁移来源，不用于新部署配置。
- NAS 操作继续暂停；现有 MySQL 容器的数据目录不能通过更改环境变量自动更名，文档列明迁移方式。

## 0.0.2 — 2026-09-26

### 权限和账号
- 私聊附件只允许消息发送者/收件人下载，不再以“认识收件人”放行第三方；群文件要求当前有效群成员。
- 管理员预留邮箱禁止公开注册，避免未验证邮箱获取管理员权限。
- 禁用/强制下线立即清除 REST token；新登录替换旧 token，旧凭据立即失效。
- 用户 API JSON 与 toString 不再输出密码摘要。
- Boot 3 控制器采用 Jakarta Validation，文本长度和历史分页限制实际生效；普通文件上传不强制提供图片封面。

### 消息和文件
- 解散群移除群成员集合，不关闭成员其他会话的连接。
- 旧连接断开不删除新连接；未认证断开不写入空用户状态；采用固定 channel 用户属性。
- 初始同步按最近三天时间戳过滤，避免把时长误当时间戳而读取过多历史。
- 单聊转发复制 DTO，保留初始化/上传通知标识，避免共享载荷被改写。
- 修正联系人缓存重复前缀；支持 token 查询参数的标准解码及多个参数场景。
- 附件/头像路径拒绝目录穿越；不存在的文件/消息返回可控业务错误，下载不再吞掉异常。
- 上传按真实文件名和大小更新元数据，无扩展名文件也执行大小限制。
- AI 回复使用插入生成的消息 ID，避免并发 SELECT 最新消息取错 ID。
- 资料局部更新和缺失头像封面不再触发空指针。

### 容器和配置
- Redisson 使用与 Spring Redis 一致的密码、ACL 用户、数据库和 TLS，初始化失败明确停止。
- 启动检查关闭探测数据库连接，依赖失败停止启动。
- 配置改为环境变量，清除配置中的本机路径和固定连接凭据。
- AI 默认禁用，可无 AI 密钥运行普通聊天。
- 新增只读、非 root Dockerfile/Compose、持久化文件目录和 DB/Redis/WebSocket 就绪检查。
- README 重写构建、NAS 部署、App/Web 接入、数据库初始化缺口与兼容边界。
- 发布 JAR 和 NAS 构建 ZIP，附测试统计、Git 提交与 SHA256。

### 验证范围
补充本机实际 MySQL schema 导出的空表初始化 SQL、可重用导出脚本、独立 MySQL/Redis Compose 与随机凭据准备脚本。新数据库不迁移任何私有用户记录；数据库初始化随首次空目录启动执行。

隔离测试覆盖附件权限、会话生命周期、Jakarta 参数校验、管理员预留邮箱、密码字段序列化、启动资源释放、Redis 配置和 AI 禁用。未接触真实 NAS、用户数据库/Redis；Docker 镜像和真实端到端部署需另行验证。
